{
  "name": "escalamento",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && cd gerenciador-equipe && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "html2pdf.js": "^0.12.1",
    "pg": "^8.13.1"
  }
}
