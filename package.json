{
  "name": "escalamento",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && cd gerenciador-equipe && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}